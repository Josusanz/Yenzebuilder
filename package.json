{
  "name": "yenzehtml",
  "version": "1.0.0",
  "description": "YENZE HTML Builder - Create and deploy HTML sites instantly",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "stripe": "^14.10.0",
    "micro": "^10.0.1"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}
